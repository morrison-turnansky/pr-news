# PR Review Filter Configuration

# GitHub repository to analyze
repository: "pytorch/pytorch"

# Skill file paths for domain knowledge
skill_paths:
  - "/workspaces/pytorch-devcontainers/.claude/skills/pytorch-dynamo/SKILL.md"
  - "/workspaces/pytorch-devcontainers/.claude/skills/pytorch-inductor/SKILL.md"

# PR filter criteria (optional - override in test_filter_dynamo.py)
filter:
  # Labels to filter by (e.g., ["module: dynamo"])
  labels: []
  # Authors to filter by (e.g., ["username1", "username2"])
  authors: []
  # Date range (ISO format: YYYY-MM-DD or YYYY-MM-DDTHH:MM:SS)
  # created_after: "2026-04-20"
  # created_before: "2026-04-24"
